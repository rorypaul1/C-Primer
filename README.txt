To run a c program:
    1. gcc -o executable_name c_program_name
    2. ./executable_name

To use the makefile:
    MinGW32-make
    then ./..exe
** You must recompile after any change! **

Debugger:
    gcc -g -o buggy buggy.c
    gdb buggy